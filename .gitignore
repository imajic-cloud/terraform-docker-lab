# Terraform working directory
.terraform/

# Terraform state files
*.tfstate
*.tfstate.*
terraform.tfstate
terraform.tfstate.backup

# Terraform plan files
tfplan

# Local variable values (may contain secrets)
terraform.tfvars
*.auto.tfvars

# Provider lock file (optional for learning repo)
.terraform.lock.hcl